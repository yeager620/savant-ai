#!/bin/bash

# Convenience wrapper for video daemon control
# Usage: ./sav-video {start|stop|restart|status|logs|list|test}

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/scripts/video/savant-video-control.sh" "$@"